<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
      <div id="error_explanation" class="alert alert-danger alert-dismissable"  >
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h4>

      <ul>
      <% @contact.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <p>
    We are happy to answer any queries you have regarding our summer course and offer consultation on how this course will benefit you. 
  </br></br>
  Please email us the following and we will get back within 24 hours:
  </br></br>
  </p>


  <div class="message">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: "input" %>
  </div>
  <div class="message">
    <%= f.label :email_address %><br>
    <%= f.text_field :email_address, class: "input" %>
  </div>
  <div class="message">
    <%= f.label :message %><br>
    <%= f.text_area :message, class: "textarea" %>
  </div>
  <div class="actions">
    <%= f.submit "Send" %>
  </div>
<% end %>
